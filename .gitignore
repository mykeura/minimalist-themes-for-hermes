# Paletas fuente (privadas, no se versionan)
palettes.json
paletas-minimalist.json

# Artefacto regenerable
minimalist-hermes-themes.zip

# Capturas de diagnóstico (no se versionan)
*.png

# Herramienta propia de construccion (no se versiona)
scripts/
